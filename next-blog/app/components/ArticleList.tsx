type BlogContent = {
    id: number;
    title: string;
    body: string;
};

type PageProps = {
    blog: BlogContent[];
};

export default function ArticleList() {
    return (
        <div>
            
        </div>
    );
}